
// This component is not used in the final version as notifications are handled inside the context,
// but it is good practice to have a dedicated component.
// I will leave this file here as a placeholder for future extension.
// The notification logic is currently handled directly within useChatManager for simplicity.
import React from 'react';

const Notification: React.FC = () => {
  return null;
};

export default Notification;
